function add(a, b) {
    return a + b ;
}

let a = add(2, 3);
console.log(`the sum is ${a}`);

//arrow functions----

const mul = (a, b) => {
    return a*b;
}

console.log(`the product is ${mul(2, 3)}`);